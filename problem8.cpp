#include <vector>
#include <array>
#include <fstream>
#include <string>
#include "util.h"


int main() {

    // Make "visible from {left, right, up, down}" maps.
    // Combine maps with `or`.

    return 1;
}
